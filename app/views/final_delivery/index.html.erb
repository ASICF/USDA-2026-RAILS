<%= react_component("Forms/FinalDelivery", {
    # states: @states,
    sl_states: @sl_states,
    nri_states: @nri_states,
    projects: @projects,
    token: form_authenticity_token, 
    # naip_folder: Rails.application.secrets.naip_final_delivery_input_path, 
    # required_folder: Rails.application.secrets.exe_end_user_path
}) %>