<%= react_component("Invoice/InvoiceForm", {
    invoice: @invoice,
    packing_slips: @packing_slips,
    selected_ps_ids: @selected_ps_ids,
    projects: @projects,
    token: form_authenticity_token,
}) %>