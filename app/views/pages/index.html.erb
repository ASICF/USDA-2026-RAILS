<% content_for :template, "large-screen" %>
<%= react_component("Dashboard", {
   states: @states,
   state_count: State.active_sl.count,
   currentUser: @current_user,
   monthRange: @month_range,

   # Statistics
   sl: @sl,
   nri: @nri,

   recentActivities: @recent_activities
}) %>