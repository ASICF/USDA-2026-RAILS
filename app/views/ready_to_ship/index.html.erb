<% content_for :template, "large-screen" %>

<%= react_component("Reports/ReadyToShip", {
    states: @states,
    projects: @projects,
    priorities: @priorities,
    token: form_authenticity_token
}) %>