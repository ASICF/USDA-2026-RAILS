<%= react_component("Forms/TileDump", {
    # states: @states,
    # projects: @projects,
    sl_states: @sl_states,
    nri_states: @nri_states,
    projects: @projects,
    token: form_authenticity_token
}) %>